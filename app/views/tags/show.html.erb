<h1>
    Articles Tagged with <%= @tag.name %>
    <%= link_to "Delete tag", tag_path(@tag), method: :delete, data: {confirm: "Do you really want to delete the tag?"}, style: "color: goldenrod; margin-left: 150px;" %>
</h1>

<ul>
    <% @tag.articles.each do |article| %>
        <li><%= link_to article.title, article_path(article) %></li>
    <% end %>
    <br><%= link_to "<< Back to Tags list", tags_path, style: "font-weight: bold;" %>
</ul>